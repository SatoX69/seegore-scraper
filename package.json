{
  "name": "seegore-scraper",
  "version": "1.0.0",
  "description": "A package for scraping seegore.com and getting the image, title, streaming link, page link (currently at 1.0.0 version, it can only scrap the pages and not searches)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "M.H",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^1.0.0",
    "axios": "^0.24.0"
  }
}